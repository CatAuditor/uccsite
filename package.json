{
  "name": "uccsite",
  "private": true,
  "description": "Utah Civic Compact — static site built by build.js, deployed on Cloudflare Pages",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx wrangler pages dev dist"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
